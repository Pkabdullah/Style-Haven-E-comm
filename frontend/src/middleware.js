
export {default } from "next-auth/middleware"
export const config={matcher:["/Cart","/Order-Tracking"]}

// import { withAuth } from "next-auth/middleware";

// export default withAuth({
//   // Define the pages to be protected
//   pages: {
//     signIn: "/login",
//   },
//   // Matcher for protected paths
//   matcher: ["/Cart", "/Order-Tracking"],
// });

// export const config = {
//   // Middleware config to be applied
//   matcher: ["/Cart", "/Order-Tracking"],
  
// };

